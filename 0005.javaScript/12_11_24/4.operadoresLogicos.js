// == -> igual que
// === -> estrictamente igual que (contenido y dato)
let numA = 3;
let numB = "3";
console.log(numA == numB); // true, porque el valor es igual
console.log(numA === numB); // false, porque el tipo de dato es diferente

// ! -> no (not)
// != -> desigual que o diferente 
// || -> o (pipe)
// && - and o Y (ampersum)
// > -> Mayor que
// >= -> Mayor o igual que
// < -> Menor que
// <= -> Menor igual que

