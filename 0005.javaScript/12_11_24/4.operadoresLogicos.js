// == -> igual que
// === -> estrictamente igual que (contenido y dato)
// ! -> no (not)
// != -> desigual que o diferente
// || -> o (pipe)
// && - and o Y (ampersum)

// > -> Mayor que
// >= -> Mayor o igual que
// < -> Menor que
// <= -> Menor igual que

